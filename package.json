{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-markdown.mjs && node scripts/generate-images.mjs && node scripts/generate-readme.mjs",
    "verify": "node --test scripts/verify.test.mjs",
    "verify:live": "node --test scripts/verify-live.test.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^5.7.0",
    "satori": "^0.33.4",
    "tailwindcss": "^4.1.0"
  }
}
